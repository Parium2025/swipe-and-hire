/**
 * One canonical public URL for every Auth splash/logo consumer.
 * The HTML splash already preloads this file, so React must reuse the same
 * request instead of emitting and decoding a byte-identical hashed copy.
 */
export const AUTH_LOGO_URL = '/parium-auth-logo.png';

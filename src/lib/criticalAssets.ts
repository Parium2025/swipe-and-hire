/**
 * Central definitions for assets that must be identical across the HTML shell
 * (index.html) and React, so preloading/decoding actually affects what we render.
 */

export const AUTH_LOGO_URL = "/lovable-uploads/parium-auth-logo.png" as const;
